

<!-- <div class="container">

 <% if user_signed_in? || current_user %>
    <p><%= link_to '+ Task', new_task_path %></p>
    <h1>Hello <%= current_user.email %></h1>
    <% end %>
<table>
  <thead>
    

    <tr>
      <th>id</th>
      <th>name</th>
      <th>Telefon</th>
      <th>Kaedah</th>
      <th>email</th>
      <th>no-tix</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>

        <td><%= task.id %> </td>
        <td><%= task.website_name %></td>
        <td><a href="https://wa.me/<%= task.mys_num %>" ><img src="https://www.nicepng.com/png/full/33-334867_10-apr-2015-whatsapp-button-png.png" width="30" /></a></td>
        <td><%= task.kaedah %></td>
        <td><%= task.email_client %></td>
        <td><%= task.user.email %></td>
        <td><%= task.no_tix %></td>
        <td><%= distance_of_time_in_words(task.created_at, Time.now) %> ago</td>

        <% if user_signed_in? && current_user %>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>



      </tr>
    <% end %>
    
   

  </tbody>
</table>
<%= will_paginate @tasks %>

</div> -->

<%= render 'table' %>
