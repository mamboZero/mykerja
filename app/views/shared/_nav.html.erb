
  <nav class="navbar navbar-expand-lg navbar-light custom-bg">
  <a class="navbar-brand" href="#">myKerja</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <% unless current_user && user_signed_in? %>
      <li class="nav-item">
         <%= link_to 'login', user_session_path, :class => 'nav-link' %>
      </li>
      <% else %>
      <li class="nav-item">
        <%= link_to 'logout', destroy_user_session_path, method: :delete, :class => 'nav-link' %>
      </li>
      <li>

        <% if user_signed_in? && current_user %>
         <div class="badge badge-success badge-success-alt">
    <%= link_to '+ Task', new_task_path , :class => 'nav-link' %></div>
      <h1>Hello <%= current_user.email %></h1>

    <% end %>
      </li>
      <% end %>
    </ul>
  </div>
</nav>
